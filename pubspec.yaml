name: begineer_application
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  fl_chart: ^1.1.1
  socket_io_client: ^2.0.3+1
  realm: ^20.2.0
  uuid: ^4.5.0
  path_provider: ^2.1.3
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
